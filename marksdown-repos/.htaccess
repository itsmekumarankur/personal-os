Options -Indexes
RewriteEngine On

# Route .md file requests through the markdown viewer
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^(.+\.md)$ /marksdown-repos/md.html?file=%{REQUEST_URI} [L,QSA]
