# Inference — Notes Overview

Inference Fundamentals:
- What inference is vs training
- Prefill phase vs decode phase
- Token generation mechanics
- Autoregressive generation
- Batch vs streaming inference

Performance optimization:
- KV cache — what it is and why it matters
- Paged attention (PagedAttention by vLLM)
- Continuous batching vs static batching
- Speculative decoding
- Quantization effect on inference speed
- Tensor parallelism
- Pipeline parallelism

Sampling strategies (affects output quality):
- Greedy decoding
- Temperature scaling
- Top-p (nucleus) sampling
- Top-k sampling
- Beam search

Inference economics:
- Cost per token (input vs output token pricing)
- GPU memory requirements per model size
- Throughput (tokens/sec) vs latency (time to first token)
- Batch inference for cost reduction
- Caching repeated prefixes (prompt caching)
- Model routing — small model vs large model
- Self-hosted vs API inference cost comparison

Inference engines:
- vLLM
- TensorRT-LLM
- Hugging Face TGI
- Ollama
- llama.cpp

GPU vs CPU inference:
- When CPU inference is acceptable (small models, low QPS)
- GPU tiers for inference (A10G, A100, H100)
- Multi-GPU inference (tensor + pipeline parallelism)
